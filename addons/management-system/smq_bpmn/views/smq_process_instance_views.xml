<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <!-- Assistant "Avancer" défini en premier : la vue formulaire de
         l'instance ci-dessous référence son action via %(...)d, qui doit
         donc déjà exister au moment où ce fichier est chargé (les
         enregistrements XML sont traités dans l'ordre du fichier). -->
    <record id="view_smq_process_instance_advance_wizard_form" model="ir.ui.view">
        <field name="name">smq.process.instance.advance.wizard.form</field>
        <field name="model">smq.process.instance.advance.wizard</field>
        <field name="arch" type="xml">
            <form string="Avancer l'instance">
                <field name="instance_id" invisible="1" />
                <group>
                    <field name="chosen_flow_id" />
                </group>
                <footer>
                    <button name="action_confirm" string="Avancer" type="object" class="btn-primary" />
                    <button string="Annuler" special="cancel" class="btn-secondary" />
                </footer>
            </form>
        </field>
    </record>

    <record id="action_smq_process_instance_advance_wizard" model="ir.actions.act_window">
        <field name="name">Avancer l'instance</field>
        <field name="res_model">smq.process.instance.advance.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <record id="view_smq_process_instance_tree" model="ir.ui.view">
        <field name="name">smq.process.instance.tree</field>
        <field name="model">smq.process.instance</field>
        <field name="arch" type="xml">
            <tree>
                <field name="process_version_id" />
                <field name="current_element_name" />
                <field name="res_record_display_name" string="Enregistrement lié" />
                <field
                    name="state"
                    widget="badge"
                    decoration-info="state == 'running'"
                    decoration-success="state == 'completed'"
                    decoration-muted="state == 'cancelled'"
                />
                <field name="started_by" />
                <field name="start_date" />
                <field name="end_date" />
            </tree>
        </field>
    </record>

    <record id="view_smq_process_instance_form" model="ir.ui.view">
        <field name="name">smq.process.instance.form</field>
        <field name="model">smq.process.instance</field>
        <field name="arch" type="xml">
            <form string="Instance de processus BPMN">
                <header>
                    <button
                        name="%(action_smq_process_instance_advance_wizard)d"
                        string="Avancer"
                        type="action"
                        class="oe_highlight"
                        invisible="state != 'running'"
                        groups="smq_quality.group_smq_writer"
                        context="{'default_instance_id': id}"
                    />
                    <button
                        name="action_cancel"
                        string="Annuler"
                        type="object"
                        invisible="state != 'running'"
                        groups="smq_quality.group_smq_quality_manager"
                    />
                    <field name="state" widget="statusbar" statusbar_visible="running,completed" />
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="process_version_id" readonly="1" />
                        </h1>
                    </div>
                    <group>
                        <group string="Position courante">
                            <field name="current_element_id" readonly="1" />
                            <field name="current_element_name" readonly="1" />
                            <field name="current_element_tag" readonly="1" />
                        </group>
                        <group string="Enregistrement métier lié">
                            <field name="res_model_id" readonly="state != 'running'" />
                            <field name="res_id" readonly="state != 'running'" />
                            <field name="res_record_display_name" readonly="1" />
                        </group>
                    </group>
                    <group string="Traçabilité">
                        <field name="started_by" />
                        <field name="start_date" />
                        <field name="end_date" />
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers" />
                    <field name="message_ids" widget="mail_thread" />
                </div>
            </form>
        </field>
    </record>

    <record id="action_smq_process_instance" model="ir.actions.act_window">
        <field name="name">Instances de processus</field>
        <field name="res_model">smq.process.instance</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Démarrez une instance depuis une version « En vigueur » d'un processus.
            </p>
        </field>
    </record>

</odoo>
